Start the Angular frontend dev server.

First ensure dependencies are installed, then run the dev server in the background from the `frontend/` directory:

```
cd /Users/repos/summary-feedback/frontend && npm install && ng serve --port 4200
```

Use the Bash tool with `run_in_background: true`. After launching, confirm it started and tell the user the frontend is running at http://localhost:4200.
